# Replay sibling for notebook.cordis.yml — swaps to the snapshot-safe base while
# retaining the same isolated notebook filesystem and content-derived versions.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ./cordis.snapshot.yml
    patches:
      - insert:
          - id: attachment-local
            name: '@deepseek-ai/dsh-attachment-local'
          - id: notebook-snapshot
            name: cordis:group
            group: true
            isolate:
              fs: true
            config:
              - id: notebook-snapshot-fs
                name: './tests/fixtures/notebook-snapshot-fs.ts'
              - id: notebook
                name: '@deepseek-ai/dsh-notebook'
              - id: tool-notebook
                name: '@deepseek-ai/dsh-tool-notebook'
              - id: notebook-memory-backend
                name: './tests/fixtures/notebook-memory-backend.ts'
